impl solution{
    pub fn is_palindrome(s: String) -> bool {
        let it = s.chars().filter(|c|.c.is_aphanumeric()).map(|c|.to_accii_lowercase());

        it.clone().eq(it.rev())
        }
        }
    }
}